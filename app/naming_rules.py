


def get_dir_path(upload_obj):
    return

def get_tree_score_paths(upload_obj):
    """Get path tuple () from upload object."""
    proj_suffix = user_upload.get_local_filename()
    os.path.join(dir_path, proj_suffix + '_scores.txt')